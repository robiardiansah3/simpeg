import AppRouter from './router/AppRouter';

/**
 * Komponen utama (root) aplikasi SIMPEG.
 * Merender AppRouter untuk mengatur rute navigasi.
 *
 * @returns {JSX.Element}
 */
function App() {
  return <AppRouter />;
}

export default App;
